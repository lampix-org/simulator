import React, { Component } from 'react';

import AppBar from '../../components/AppBar';

class Core extends Component {
  render() {
    return (
      <div>
        <AppBar />
      </div>
    );
  }
}

export default Core;
